package _03inheritance;

/*
# Java 方法重写（Method Overriding）

## 是什么

方法重写（也叫**方法覆盖**）发生在**父子类之间**：子类中定义一个与父类**完全相同的方法签名**（方法名 + 参数列表）的方法，用自己的逻辑替换父类的实现。

```java
class Animal {
    public void sound() {
        System.out.println("动物发出声音");
    }
}

class Dog extends Animal {
    @Override
    public void sound() {
        System.out.println("汪汪汪");
    }
}
```


## 重写规则

| 要求 | 说明 |
|------|------|
| 继承关系 | 必须发生在子类重写父类方法时 |
| 方法名 | 必须相同 |
| 参数列表 | 必须相同（**参数不同 → 是重载，不是重写**） |
| 返回类型 | 相同或父类返回类型的子类型（协变返回） |
| 访问权限 | 不能比父类更严格（如 `public` 不能改成 `protected`），可以更宽松 |
| 异常 | 不能抛出比父类更宽泛的受检异常 |
| @Override | 推荐加上，编译器会帮你校验是否真的重写成功 |

## 关键细节

1. **谁的方法被调用看对象类型，不看引用类型**——这就是运行时多态的体现：

```java
Animal a = new Dog();   // 父类引用指向子类对象
a.sound();              // 输出：汪汪汪（动态绑定，调用 Dog 的重写版本）
```


2. **`static` 方法不能被重写**，只能被隐藏（子类定义同名 static 方法，各自按引用类型调用）。
3. **`private` 方法不能被重写**，子类可以定义相同签名的方法，但那只是自己的新方法。
4. **`final` 方法禁止被重写**。
5. 子类重写的方法可以通过 `super.方法名()` 调用父类被覆盖的版本。

## @Override
@Override：@Override是Java中的一个注解（Annotation），用于标识一个方法声明打算重写父类或接口中的方法。它是一个编译时注解，不会影响代码运行。

子类可以通过 @Override 重写父类方法，重写规则：
1. 方法名、参数列表必须相同
2. 返回值类型必须相同或是子类
3. 访问修饰符不能比父类更严格
4. 不能抛出比父类更多的异常

@Override的主要作用：
1. 编译检查：确保方法正确重写
2. 代码自文档：明确表明方法是重写的
3. 防止错误：避免方法签名写错
4. 提高可维护性：当父类方法变更时及时发现

使用建议：
1. 总是使用：只要是重写的方法，都应该加上
2. 不要滥用：只在真正重写时使用
3. 注意签名：确保方法签名完全匹配
4. 区分重载：重载方法不要用@Override

## 与重载的区别

- **重写（Override）**：父子类之间，方法签名完全一致，"覆盖"旧实现。
- **重载（Overload）**：同一个类中，方法名相同但参数列表不同，"共存"多个版本。

**一句话总结**：方法重写，就是子类不满意父类的方法，用自己的实现把它"换掉"——能力一样（方法签名），做法不同（方法体），这也是多态能实现的前提。
*/
public class Demo3 {
    public static void main(String[] args) {
        Cat cat = new Cat("招财", 3);
        cat.makeSound();
    }
}
