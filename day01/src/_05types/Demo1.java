package _05types;

// 基本数据类型
/*
类型            位数               范围
byte           8 位         (-128) ~ (127)
short          16 位        (-32768) ~ (32767)
int            32 位        (-2147483648) ~ (2147483647)
long           64 位        (-9223372036854775808) ~ (9223372036854775807)
float          32 位        (-3.4028235E+38) ~ (3.4028235E+38)
double         64 位        (-1.7976931348623157E+308) ~ (1.7976931348623157E+308)

-----------------------------------------------------

## Java 基本数据类型 vs 引用数据类型的内存存储

### 1. 基本数据类型（Primitive Types）

**包含：** `byte`, `short`, `int`, `long`, `float`, `double`, `char`, `boolean`

**存储过程：**

- **存储位置：栈（Stack）**
- 变量的值**直接**存储在栈内存中
- 声明时直接分配固定大小的内存空间（大小由类型决定，如 `int` 占 4 字节）
- 栈内存由系统自动管理，方法执行完毕即释放

```
栈内存：
┌─────────────┐
│ int a = 10  │  ← 直接存储值 10
├─────────────┤
│ double b=3.14│  ← 直接存储值 3.14
└─────────────┘
```

**特点：** 访问速度快、生命周期由作用域控制、赋值是**值拷贝**

---

### 2. 引用数据类型（Reference Types）

**包含：** 类（`class`）、接口（`interface`）、数组（`[]`）、枚举（`enum`）

**存储过程（分两步）：**

1. **栈（Stack）：** 存储引用变量（即对象的**内存地址**，类似指针）
2. **堆（Heap）：** 存储对象的**实际数据**

```
栈内存：                         堆内存：
┌─────────────────┐             ┌──────────────────┐
│ Person p = 0x123 │──地址指向──→│  name = "张三"    │
└─────────────────┘             │  age = 25        │
                                │  ...             │
                                └──────────────────┘
```

**赋值时的区别：** 引用类型赋值传递的是**地址**，多个引用可指向同一对象

```java
Person p1 = new Person("张三", 25);
Person p2 = p1;          // p2 和 p1 指向同一个堆对象
p2.setName("李四");       // p1.getName() 也会变成 "李四"
```

---

### 关键对比总结

| 对比维度 | 基本数据类型 | 引用数据类型 |
|---|---|---|
| 栈存储 | 存**值本身** | 存**地址**（指针） |
| 堆存储 | 无 | 存**实际对象数据** |
| 赋值行为 | 值拷贝（独立） | 地址拷贝（共享） |
| == 比较 | 比较值 | 比较地址（是否同一对象） |
| 默认值 | 有固定默认值（如 0, false） | `null` |
| 垃圾回收 | 方法结束自动释放 | GC 回收无引用的堆对象 |

**一句话总结：** 基本数据类型 **"人就在栈里"**，引用数据类型 **"栈里只是门牌号，人住在堆里"**。
*/
public class Demo1 {
    public static void main(String[] args) {
        // 整型
        byte a = 100;
        short b = 1000;
        int c = 10000;
        long d = 10000000000L;

        // 浮点类型
        float e = 0.1f; // 单精度
        double f = 0.1; // 双精度

        // 字符
        char g = 'e';
        // 字符串它是引用数据类型，后面聊
        String h = "fff";

        // 布尔
        boolean flag1 = true;
        boolean flag2 = false;
    }
}
