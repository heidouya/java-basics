package _02collections;

/*
# 哈希表（Hash Table）简介

哈希表是一种 "**以空间换时间**" 的数据结构，核心思路是：**通过哈希函数直接算出元素存储的位置，实现 O(1) 的查找。底层是「数组 + 解决哈希冲突」**

## 核心概念
- 哈希函数：hash(key)，把任意 key 转为数组索引。
- 哈希冲突：不同 key 算出相同数组下标，多个元素要放在同一个位置。
- 负载因子 loadFactor：元素数量 / 数组容量，达到阈值就扩容，降低冲突概率。

## 核心结构（以 Java 的 HashMap 为例）

```java
数组 + 链表（Java 8 后链表过长会转红黑树）
```
```mermaid
graph TD
    A["数组（桶 bucket）"] --> B["链表/红黑树"]
    A --> C["链表/红黑树"]
    A --> D["链表/红黑树"]
```

## 工作流程

### 存储（put）
```java
map.put("张三", 20);
```

1. 对 key 计算 hash 值，得到数组下标
2. 下标位置为空，直接存入
3. 下标不为空，发生冲突（哈希冲突）：
   - key 完全相等，覆盖旧 value
   - key 不等，挂到链表；链表过长转红黑树
4. 元素数量 > 容量 * 0.75，数组扩容为原来 2 倍，所有元素重新哈希迁移

简单来说就是：算 hash→算下标→桶为空直接放；碰撞就挂链表；链表到 8 转红黑树。

### 查找（get）
- 同样算出下标 → 直接跳到桶里 → 在链表上逐个比较 `equals` 找到目标

1. 计算 key 的 hash 值
调用 key.hashCode()，再经过扰动函数（高位混合）得到最终 hash。

2. 定位数组下标（桶位置）
index = hash & (table.length‑1)，直接算出该 key 在数组的桶下标，跳到对应桶。

3. 判断桶位置元素：
✅ 桶为空：直接返回 null。
✅ 桶头结点就是目标：先比较 hash相等 && (== 或者 equals相等)，命中直接返回 value。
✅ 桶后面是链表：循环遍历链表每一个 Node，依次对比：
hash相同 && (key == node.key || key.equals(node.key))
逐个比对，找到就返回 value；遍历完没找到返回 null。
✅ 已经树化（红黑树）：不再走链表逐个遍历，调用树的查找方法，按照树规则快速搜索，O (logn)。

简单来说就是：算 hash→算下标→桶空返回 null；头节点匹配直接返回；链表遍历 equals；红黑树树查找。

❓为什么先要比 hash？
hash 不一样，key 一定不一样，就不用调用开销更大的 equals()，做快速过滤。
规则：hash 不等 → key 一定不等；hash 相等 → key 不一定相等（哈希碰撞），必须再 equals

❓为什么不能只用 hashCode，必须还要 equals？
不同对象可以 hashCode 相同（哈希碰撞），hashCode 只是定位桶，真正判断 key 相等靠 equals。

## 关键概念

| 概念 | 说明 |
|------|------|
| 哈希函数 | 把 key 映射成数组下标，Java 中基于 `hashCode()` |
| 哈希冲突 | 不同 key 算出相同下标，用**链地址法**解决（链表挂后面） |
| 负载因子 | 默认 0.75，元素个数 / 数组长度 达到该值就**扩容**（翻倍） |
| 红黑树化 | Java 8 优化：链表超过 8 个节点转红黑树，最差查找从 O(n) 降为 O(log n) |

## Java 中常见实现

| 类 | 说明 |
|----|------|
| `HashMap` | 最常用，无序，允许一个 null key |
| `HashSet` | 底层就是 HashMap（只存 key），元素不重复 |
| `Hashtable` | 老版本，线程安全但性能差，已不推荐 |
| `LinkedHashMap` | 保持插入顺序 |
| `ConcurrentHashMap` | 线程安全的升级版 |

## 知识联系

| 对比 | TreeSet / TreeMap | HashSet / HashMap |
|------|-------------------|-------------------|
| 底层结构 | 红黑树 | 哈希表（数组+链表） |
| 排序 | 自动排序（用 `compareTo`） | **无序** |
| 去重依据 | `compareTo == 0` | `hashCode` + `equals` |
| 增删查时间复杂度 | O(log n) | 平均 O(1) |

## 简单记忆

**哈希表 = 数组的"直接寻址" + 链表的"冲突兜底"**

- 定位快：算完哈希直接跳下标，不用一个个找
- 去重靠 `hashCode` 和 `equals` 共同作用：先比哈希（判断桶），再比 equals（判断链表上是否相同）

> ⚠️ 注意：自定义对象放进 `HashSet`/`HashMap` 时，**必须同时重写 `hashCode()` 和 `equals()`**，否则去重和查找会失效——这和你自定义对象放进 `TreeSet` 必须实现 `Comparable` 是同一个道理。
*/
public class Demo10 {
}
